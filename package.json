{
  "name": "pix-llm",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:db": "bun --watch ./packages/db-server/index.ts",
    "dev:preview": "cd ./packages/preview-client && bunx --bun vite",
    "dev": "concurrently 'bun dev:db' 'bun dev:preview'"
  },
  "workspaces": [
    "packages/*"
  ]
}
